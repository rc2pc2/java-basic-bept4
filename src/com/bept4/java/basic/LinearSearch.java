package com.bept4.java.basic;

import java.util.Scanner;

public class LinearSearch {
    
    public static void main(String[] args) {
        
        // int[] integers = { 1, 2, 3, 4, 5 };
        // Scanner input = new Scanner(System.in);
        
        // System.out.println("Type a number to search and press enter...");
        // int userNumber = input.nextInt();

        // // condizione if che controlli se il numero inserito sia uguale a quello dell'array
        // // # puoi creare un ciclo for per poter iterare su ogni elemento
        // // % puoi creare un ciclo while per poter iterare su ogni elemento


        // for (int i = 0; i < integers.length; i++) {
        //     // < PER OGNI INTERO PRESENTE NELL'ARRAY
        //     // > controllo se il numero dell'utente sia uguale all'elemento iterato
        //     if ( integers[i] == userNumber){
        //         System.out.println("L'ho trovato all'indice " + i);
        //     } else {
        //         System.out.println("Non l'ho trovato");
        //     }
        // }

        // boolean isFound = false; 

        // int j = 0;
        // while (isFound == false && j < integers.length){
        //     if ( integers[j] == userNumber){
        //         System.out.println("L'ho trovato all'indice " + j);
        //         isFound = true;
        //     } else {
        //         System.out.println("Non l'ho trovato");
        //     }

        //     j++;
        // }

        String stringa = "ciao io sono una stringa";

        System.out.println(stringa.charAt(0) == 'c');
    }
}
